// Firestore Security Rules for SmartAss
// Copy these into Firebase Console > Firestore > Rules

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Usernames — unique index
    match /usernames/{username} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }

    // Global fact stats (known counts) — any logged-in user can read/write
    match /factStats/{factKey} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }

    // Users can read/write their own profile
    match /users/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;

      // Saved facts — only owner
      match /saved/{factId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }

      // "Already knew this" — only owner
      match /known/{factKey} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }

      // Inbox — owner can read/delete, anyone logged in can add
      match /inbox/{msgId} {
        allow read, delete: if request.auth != null && request.auth.uid == userId;
        allow create: if request.auth != null;
      }
    }
  }
}
